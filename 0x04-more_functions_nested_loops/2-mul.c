#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: the first number to multiply
 * @y: the second number to multiply with the first number
 * Return: The results of multiplication between the x param and the y param
 */
int mul(int x, int y)
{
	return (x * y);
}
